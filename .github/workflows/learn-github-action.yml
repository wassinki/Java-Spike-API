name: learn-github-actions
run-name: building and testing the mindstorms api
on: [push]
jobs:
  build-project:
    steps:
      - run: ./gradle build
  test-project:
    steps:
      - run: ./gradle test
